[Unit]
Description=start postinst script upon first boot
Requires=postinst-tedge.service
After=postinst-tedge.service

[Service]
Type=simple
ExecStart=/bin/sh -c 'sleep 1 ; /usr/sbin/tedge-mapper/postinst'

[Install]
WantedBy=multi-user.target